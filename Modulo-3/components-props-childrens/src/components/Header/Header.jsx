import { Title } from "../Title/Title"
import "./Header.css"

export const Header = () => {
    return (
        <header>
            <Title texto="Componetizando en header, main y footer"/>
        </header>
    )
}